/**
 * core/db/schema.ts
 *
 * Barrel file that re-exports all module schemas for app-side typing.
 * As each module's schema.ts is written, import and re-export its tables here.
 * Example:
 *   export { bookings, roomAllocations, bookingNights, bookingGuests } from '@/modules/bookings/schema';
 */
export const schema = {};