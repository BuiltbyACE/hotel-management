/**
 * modules/frontdesk/schema.ts
 *
 * Frontdesk orchestrates check-in, check-out, walk-ins, no-shows, and room
 * moves through bookings + availability. It owns NO DDL tables; all state
 * lives on bookings, room_allocations, and folio_charges in other modules.
 *
 * This file is intentionally empty. [1:1 MIRROR — no tables to mirror]
 */
export {};
