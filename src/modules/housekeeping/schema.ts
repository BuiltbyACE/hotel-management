/**
 * modules/housekeeping/schema.ts
 *
 * The housekeeping module reads/writes rooms.housekeeping_status (owned by
 * the property module). It owns NO DDL tables. The room_status_board is a
 * read projection over the rooms table.
 *
 * This file is intentionally empty. [1:1 MIRROR — no tables to mirror]
 */
export {};
